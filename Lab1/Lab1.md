Connect the workspace to a Git repository (Git folder) and commit from day one. 

Attach a notebook to the shared cluster and run it — this is the only compute available to you. 

Import data into Databricks (CSV / JSON). 

Load the data into a Delta table using a Python notebook, saving it to your schema: 
General login>.<table>. 

Practice basic Spark DataFrame operations (select, filter, join, groupBy). 

Build a basic dashboard over your table. 

Load data from an external API. 

Shared-cluster etiquette: the cluster is shared with the whole cohort and auto-terminates, so don't rely on long-running state and keep notebooks re-runnable. 